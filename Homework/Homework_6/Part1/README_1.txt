Author:
    Matthew Reynolds. I didn't have any help on this assignment outside of the provided code.

Purpose:
    make an operand overload functions that adds 2 matrices and returns an object. make an operand overland function
    for << to print a matrix. make an operand overload function that adds a float to a matrix. and make an operand
    overload function that adds a matrix to a float

How it works:
    

How to run & cleanup:
    use the commands from the makefile, make & clean.

    make command is this:
    g++ -Wall run_matrix.cpp Matrix.cpp -lm –o runM

    clean command is this:
    rm runM